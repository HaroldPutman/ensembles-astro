---
import FullWidthLayout from '../layouts/FullWidthLayout.astro';
import { Icon } from 'astro-icon/components';
---

<FullWidthLayout title="Ensembles - Affordable Group Music and Art Classes">
  <div class="hero-container">
    <section class="hero-one">
      <h1>Affordable Group Music and Art Classes</h1>
      <div class="hero-image primary">
        <img
          src="/heroes/classes.jpg"
          alt="Students working on art projects together"
          loading="eager"
        />
      </div>
      <div class="cta-container">
        <a href="/activities?kind=class" class="cta-button oswald-cta">
          Sign Up Now
          <Icon name="mdi:arrow-right-thick" width="28" height="28" />
        </a>
      </div>
    </section>
    <section class="hero-two">
      <h2>Performance Groups</h2>
      <div class="hero-image secondary">
        <img
          src="/heroes/groups.jpg"
          alt="Jazz band performing outdoors"
          loading="lazy"
        />
      </div>
      <div class="cta-container">
        <a
          href="/activities?kind=group"
          class="cta-button cta-button-inverse oswald-cta"
        >
          Join Now <Icon name="mdi:arrow-right-thick" width="28" height="28" />
        </a>
      </div>
    </section>
    <section class="hero-three">
      <h2>Special Events</h2>
      <div class="hero-image secondary">
        <img
          src="/heroes/events.jpg"
          alt="Art gallery paint pouring event"
          loading="lazy"
        />
      </div>
      <div class="cta-container">
        <a href="/activities?kind=event" class="cta-button oswald-cta">
          Learn More <Icon
            name="mdi:arrow-right-thick"
            width="28"
            height="28"
          />
        </a>
      </div>
    </section>
  </div>
</FullWidthLayout>

<style>
  /* Hero Section */
  .hero-container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    min-height: 450px;
  }

  .hero-one {
    grid-column: 1 / 3;
    background-color: var(--color-background-secondary);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
  }
  .hero-one h1 {
    color: var(--color-text-secondary);
    padding: 1rem;
  }
  .hero-one .cta-container {
    padding: 1rem;
  }
  .hero-two h2,
  .hero-three h2 {
    text-align: center;
    font-size: 2rem;
  }

  .hero-three h2 {
    color: var(--color-text-tertiary);
  }

  .hero-three {
    background-color: var(--color-background-tertiary);
  }

  .hero-image.primary {
    order: -1;
    grid-row: 1/3;
    text-align: center;
  }
  .hero-image.primary img {
    display: block;
    margin: 0 auto;
    max-width: 100%;
    max-height: 500px;
  }

  .hero-image.secondary img {
    display: block;
    margin: 0 auto;
    width: 80%;
    border-radius: 24px;
  }

  .hero-two .cta-container,
  .hero-three .cta-container {
    text-align: center;
    padding: 1rem;
  }

  /* CTA Button */
  .cta-button {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    background: var(--color-background-secondary);
    color: var(--color-text-secondary);
    padding: 0.875rem 1.5rem;
    font-size: 1.1rem;
    font-weight: 700;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    border: 3px solid var(--color-text-secondary);
    transition: all 0.2s ease;
  }

  .cta-button:hover {
    background: var(--color-text-secondary);
    color: var(--color-background-secondary);
  }

  .cta-button.cta-button-inverse {
    background: var(--color-text-secondary);
    color: var(--color-background-secondary);
    border-color: var(--color-background-secondary);
  }

  .cta-button.cta-button-inverse:hover {
    background: var(--color-background-secondary);
    color: var(--color-text-secondary);
  }

  /* Mobile Styles */
  @media (max-width: 768px) {
    .hero-container {
      grid-template-columns: 1fr;
      min-height: auto;
    }
    .hero-one {
      grid-column: 1 / 2;
    }
  }
  @media (max-width: 648px) {
    .hero-one {
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      position: relative;
      min-height: 600px;
    }

    .hero-one h1 {
      position: relative;
      z-index: 1;
      text-align: center;
      padding: 1rem 0.5rem;
      font-size: 1.5rem;
      background-color: var(--color-background-secondary);
    }
    .hero-one .cta-container {
      position: relative;
      z-index: 1;
      text-align: center;
      padding: 1rem 1rem 4rem 1rem;
    }

    .hero-two h2,
    .hero-three h2 {
      font-size: 1.5rem;
    }

    .hero-image.primary {
      position: absolute;
      inset: 0;
      order: unset;
      grid-row: auto;
      margin: 0;
      width: 100%;
    }
    .hero-image.primary img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      max-height: none;
    }
  }
</style>
